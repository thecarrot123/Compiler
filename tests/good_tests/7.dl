(func pp (a b)
    (setq a 5)
    (cons a b))

(prog (a)
    (while true
        (setq a (plus a 2))
        (cond (greater a 10) (break)))
    (return a)
    (while (less a 14)
        (setq a (plus a 2)))
    a)